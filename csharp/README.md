# C# kernel (frozen at 2.2.1)

This is the 2013–2015 C# 5 / .NET 4.5 library. It is not
the default kernel. See the repository root for 3.0 (Rust).

Open `KHGraphDB.sln` in Visual Studio 2013 only if you need
the fossil.
