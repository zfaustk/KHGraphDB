# Derived

The recipe lives on the Type. Members
are a posting. Rebuild is a walk.
Compact drops orphans.

MARK VIEW names a query. That text is
schema. A hit cannot write it. Change
the recipe, compact drops the soup.

A hit cites an address. It does not
copy the page. The hop is DERIVED_FROM.
Type is still an object. Cosine, if
any, stays at home.

A reason is content. The B-tree
refuses it. The trusted part is the
edge and the Pos. Prose is not an
index.

A pin does not see a later fill.
Fold, if it comes, is bounded.

The notebook writes what it found
the same way it writes a vertex.
The formula is Type. The soup is
members. Do not start a second
engine for a view.

The soup is not this file.
