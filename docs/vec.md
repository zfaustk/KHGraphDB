# Vec

The page is being asked to retrieve.
Retrieval is FIND. An embedding is a
posting. It is not the vertex. It is
not the page.

MARK VECTOR is MARK CONTENT for a
different payload. The B-tree refuses
it. The WAL does not hold the floats.
A file `vec/{id}-{serial}` does. Serials
do not overwrite. A pin names the old
file. Compact drops orphans. Drop the
directory and the graph still opens.
Rebuild is a walk.

Cosine is a scan of the type. Unique
was one. Similar is the members. There
is no HNSW. There is no model. A crate
that approximates would be a second
truth. The notebook is the set.

SIMILAR seeds from that posting. It is
not a walk. EXPLAIN cost is the type
length. Home still owns the page. A
far cite does not carry the vector.
The vector stays with the address,
same rule as the body.

People will put a model in the
database. The model is not ours.
